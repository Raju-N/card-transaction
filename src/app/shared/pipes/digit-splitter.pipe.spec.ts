import { DigitSplitterPipe } from './digit-splitter.pipe';

describe('DigitSplitterPipe', () => {
  it('create an instance', () => {
    const pipe = new DigitSplitterPipe();
    expect(pipe).toBeTruthy();
  });
});
